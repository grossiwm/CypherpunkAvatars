forge script script/CypherpunkAvatars.s.sol:CypherpunkAvatarsScript \
    --rpc-url $RPC_URL \
    --chain-id $CHAIN_ID \
    --build-info \
    --broadcast \
    --verbosity \
    --private-key $PRIVATE_KEY
